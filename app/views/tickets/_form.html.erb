<%= form_with model: @ticket,local: true do |form| %>
 <!-- Validations Section -->
 <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@ticket.errors.count, "error") %> prohibited
        this ticket from being saved:
      </h2>
      <ul>
        <% @ticket.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <!-- Validation Section Ends Here -->

  <!-- Add Data -->
 <p>
  <%= form.label :SeatNo %><br />
  <%= form.text_field :seatno, class: "form-control",placeholder: "Seat No"  %>
</p>


 <p>
  <%= form.label :class_type %><br />
  <%= form.text_field :class_type, class: "form-control",placeholder: "Class Type"  %>
</p>


 <p>
  <%= form.label :source %><br />
  <%= form.text_field :source, class: "form-control",placeholder: "Source"  %>
</p>

 <p>
  <%= form.label :destination %><br />
  <%= form.text_field :destination, class: "form-control" ,placeholder: "Destination"  %>
</p>

 <p>
  <%= form.label :Flight %><br />
  <%= form.select :flight_id, Flight.all.collect { |f| [ f.name , f.id ]  } %>
</p>

<p>
	<%= form.submit class: "btn btn-outline-success" %>
</p>

 <% end %>